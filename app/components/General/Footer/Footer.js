import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;
const AppFooter = () => {
  return <Footer style={{ textAlign: 'center' }}>页脚占位</Footer>;
};
export default AppFooter;
